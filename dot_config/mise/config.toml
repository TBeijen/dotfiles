[tools]
# Intent-level constraints. Exact versions + checksums live in mise.lock.
claude = "2"
fnox = "1"
jq = "1"
kubectl = "1.34"
node = "26"
python = "3.14"
starship = "1"

[settings]
lockfile = true
experimental = true
